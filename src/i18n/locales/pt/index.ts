import type { DeepPartial } from '../../types';
import type { Messages } from '../../catalogs';

/**
 * Portuguese — registered but not yet translated.
 *
 * See `../es/index.ts` for the (identical, content-only) steps to complete it.
 */
export const pt: DeepPartial<Messages> = {};

export default pt;
