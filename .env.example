# Wi-Vendor (Wi-Mall vendor dashboard) — environment variables (Vite).
# Copy to `.env` and fill in.
#
# ─── Production hosts ─────────────────────────────────────────────────────────
# The values below are the local-development defaults. In production this app is
# served at https://vendor.wi-mall.com and the two origins it talks to are:
#
#   VITE_API_BASE_URL=https://api.wi-mall.com/api
#   VITE_STOREFRONT_BASE_URL=https://wi-mall.com
#
# The rest of the platform, for reference — none of it is called from here:
#   agency dashboard  https://agency.wi-mall.com
#   agent web app     https://agent.wi-mall.com
#
# Note the hyphen: `wimall.com` is a different company's domain and must never
# be used anywhere.

# Base URL of the backend API (defaults to http://localhost:8022/api when unset).
VITE_API_BASE_URL=http://localhost:8022/api

# Base URL where uploaded files are served (defaults to the API origin /uploads).
# VITE_FILE_BASE_URL=

# Origin of the customer-facing storefront (the `landing` app). Used to build the
# public links a vendor copies or shares, the storefront preview iframe, and the
# sign-in redirect on /login. Defaults to the landing dev server when unset. In
# production this is the main site — https://wi-mall.com — NOT the API origin.
VITE_STOREFRONT_BASE_URL=http://localhost:3000

# Stripe publishable key (pk_test_… / pk_live_…). Enables card payments for plan
# upgrades and credit top-ups. When unset, the billing UI offers mobile money only.
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_51Tl7mmBKKsBJNpBM0qQhxtwhErakLmTWGXJldimCmBLZffb54KCFchgDbMtpkdra7VLP00rr9WE9Gxqjjqe6z0XB00dsQQ2N6P

# ─── Push notifications (FCM web push) ────────────────────────────────────────
# Firebase Web SDK config for the MESSAGING Firebase project (separate from the
# file-storage project). Get these from: Project settings → General → Web app,
# and the VAPID key from Cloud Messaging → Web Push certificates → Key pair.
# When ANY of these are unset, push stays inert (in-app notifications still work).
# NOTE: keep these in sync with the hardcoded config in
# public/firebase-messaging-sw.js (the service worker can't read Vite env vars).
VITE_FIREBASE_API_KEY=jdqkldqd
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
VITE_FIREBASE_VAPID_KEY=
