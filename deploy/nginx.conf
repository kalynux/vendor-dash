# Serving a Vite single-page app. Three jobs: hand back index.html for routes that
# exist only in the browser, cache the hashed assets hard and index.html not at
# all, and refuse to pretend a missing file is a page.
#
# Copied into the image as /etc/nginx/conf.d/app.conf, replacing the stock
# default.conf (which has no SPA fallback and no cache policy).

server {
    listen      80;
    listen      [::]:80;
    server_name _;

    root  /usr/share/nginx/html;
    index index.html;

    # Traefik terminates TLS and talks to this container over plain HTTP on the
    # private dokploy-network. Logging the forwarded address rather than Traefik's
    # container IP is the difference between a useful access log and a column of
    # identical internal addresses.
    set_real_ip_from 10.0.0.0/8;
    set_real_ip_from 172.16.0.0/12;
    set_real_ip_from 192.168.0.0/16;
    real_ip_header   X-Forwarded-For;
    real_ip_recursive on;

    server_tokens off;

    # ─── Compression ─────────────────────────────────────────────────────────
    # Text compresses by roughly 3-4x and this is the cheapest win available on a
    # 2 vCPU host. Images and fonts are already compressed — listing them would
    # spend CPU to make them very slightly larger.
    gzip              on;
    gzip_vary         on;
    gzip_comp_level   6;
    gzip_min_length   1024;
    gzip_proxied      any;
    gzip_types
        text/plain
        text/css
        text/javascript
        application/javascript
        application/json
        application/manifest+json
        image/svg+xml;

    # ─── Baseline response headers ───────────────────────────────────────────
    # ⚠ NO Content-Security-Policy HERE, DELIBERATELY. A CSP that is wrong in
    #   production fails by making the app blank, and the right policy for this
    #   bundle is not knowable from a config file — it depends on which inline
    #   styles the component library emits and which origins the app fetches.
    #   Adding one is deliberate work with a staging pass behind it, not a line to
    #   guess at during a first deploy.
    add_header X-Content-Type-Options "nosniff"                         always;
    add_header Referrer-Policy        "strict-origin-when-cross-origin" always;
    # Nothing embeds this dashboard. It is the app that embeds another — the
    # storefront preview iframe points OUT at wi-mall.com, which is exactly why
    # the landing site deliberately sends no framing header of its own.
    add_header X-Frame-Options        "DENY"                            always;

    # ─── Hashed build output ─────────────────────────────────────────────────
    # Vite fingerprints every file in /assets, so the contents of a given name can
    # never change. Cache for a year and never revalidate.
    #
    # ⚠ try_files $uri =404 IS THE IMPORTANT LINE, NOT THE CACHING.
    #   Without it these requests fall through to the SPA fallback below and a
    #   MISSING script is answered with index.html — 200 OK, Content-Type
    #   text/html, which the browser refuses to execute as a module. The symptom is
    #   a blank page, a console error about MIME types, and a perfectly clean
    #   server log. A 404 here says what actually happened.
    location /assets/ {
        try_files   $uri =404;
        expires     1y;
        add_header  Cache-Control "public, max-age=31536000, immutable" always;
        access_log  off;
    }

    # Same reasoning for files that carry no fingerprint: a typo in an icon path
    # should 404, not silently return the whole application.
    location ~* \.(?:png|jpe?g|gif|svg|ico|webp|avif|woff2?|ttf|eot|map)$ {
        try_files   $uri =404;
        expires     30d;
        add_header  Cache-Control "public, max-age=2592000" always;
        access_log  off;
    }

    # ─── Service worker ──────────────────────────────────────────────────────
    # ⚠ A CACHED SERVICE WORKER IS A STUCK SERVICE WORKER. The browser keeps using
    #   the installed copy until it fetches a BYTE-DIFFERENT file, so an
    #   intermediary caching this for a day pins push notifications to a day-old
    #   build. Unlike everything in /assets it carries no fingerprint — the
    #   filename is fixed, because it is the path registered in the source — so
    #   there is no hash to change and nothing but these headers to rely on.
    #
    # ⚠ It also holds a SECOND, hardcoded copy of the Firebase web config, because
    #   a service worker cannot read Vite env vars. That copy drifts silently:
    #   registration succeeds and nothing is ever delivered. env/.env.production carries
    #   the same warning — this is the other end of it.
    location = /firebase-messaging-sw.js {
        try_files  $uri =404;
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        expires    -1;
    }

    # ─── The document ────────────────────────────────────────────────────────
    # index.html names the current hashed bundle, so a cached copy is how a browser
    # keeps loading the PREVIOUS release after a deploy — and because the old
    # assets are gone, that is a blank page rather than a stale one. It must never
    # be cached: not by the browser, not by a proxy.
    location = /index.html {
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        add_header Pragma        "no-cache"                            always;
        expires    -1;
    }

    # ─── Everything else is a client-side route ──────────────────────────────
    # React Router owns the paths. The server has no such files, so every unmatched
    # path gets the document and the router decides — including the 404 page, which
    # is why this is a 200 and not an error.
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Container-local liveness probe, kept out of the access log so the healthcheck
    # does not bury real traffic.
    location = /healthz {
        access_log off;
        add_header Content-Type text/plain always;
        return 200 "ok\n";
    }
}
